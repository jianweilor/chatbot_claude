streamlit>=1.38
pandas>=2.0
openpyxl>=3.1
requests>=2.31
beautifulsoup4>=4.12
anthropic>=0.40   # Claude-assisted search interpretation + recommendations (utils/claude_assistant.py) — optional, only used if an API key is entered

# Only needed for Step 1's live scraping (sgcarmart_url.py, spec_url_scraper.py,
# pricing_url_scraper.py). These will not work on Streamlit Community Cloud
# (no browser/network egress) — install locally or on a host with Chrome.
selenium>=4.20
webdriver-manager>=4.0
